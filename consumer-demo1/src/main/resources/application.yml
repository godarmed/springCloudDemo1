server:
  port: 8190
spring:
  application:
    name: consumer-8190


eureka:
  client:
    service-url:
      defaultZone: http://server1:8781/eureka/

#hystrix的超时时间
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 9000
#ribbon的超时时间
ribbon:
  ReadTimeout: 3000
  ConnectTimeout: 3000

